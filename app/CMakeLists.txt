FILE(GLOB APPSRC "${PROJECT_SOURCE_DIR}/app/main.cpp")

ADD_EXECUTABLE(${PROJECT_NAME} ${APPSRC})
SET_TARGET_PROPERTIES(${PROJECT_NAME} PROPERTIES ENABLE_EXPORTS ON)
TARGET_LINK_LIBRARIES(${PROJECT_NAME}
    smuggler
    ${CMAKE_DL_LIBS}
    ${CONAN_LIBS}
)
